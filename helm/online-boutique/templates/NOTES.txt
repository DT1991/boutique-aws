Online Boutique has been deployed!

Release : {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}
Chart    : {{ .Chart.Name }}-{{ .Chart.Version }}
Tag      : {{ .Values.global.tag }}
Env      : {{ .Values.global.environment }}

=== Access ===
{{- if .Values.ingress.enabled }}
URL: https://{{ .Values.ingress.host }}
{{- else }}
Run the following to port-forward frontend locally:
  kubectl port-forward svc/frontend 8080:80 -n {{ .Release.Namespace }}
Then open: http://localhost:8080
{{- end }}

=== Useful commands ===
# Check all pods
kubectl get pods -n {{ .Release.Namespace }}

# Check helm release history
helm history {{ .Release.Name }} -n {{ .Release.Namespace }}

# Rollback to previous version
helm rollback {{ .Release.Name }} -n {{ .Release.Namespace }}

# Upgrade with new image tag
helm upgrade {{ .Release.Name }} {{ .Chart.Name }} \
  -n {{ .Release.Namespace }} \
  --reuse-values \
  --set global.tag=<new-tag>

=== loadgenerator ===
{{- if .Values.services.loadgenerator.enabled }}
    loadgenerator is ENABLED — synthetic traffic is being generated.
{{- else }}
loadgenerator is disabled (correct for staging/prod).
{{- end }}
